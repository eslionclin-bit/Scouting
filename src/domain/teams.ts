/** Kant van het net. Apart van `types.ts` zodat de puntentelling er geen
 * kringverwijzing voor nodig heeft. */

/** 'us' = eigen team, 'them' = tegenstander. */
export type TeamSide = 'us' | 'them';
