{
  "name": "volley-scouting",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Offline-first volleybal scouting-app — datamodel, lokale opslag en sync-laag",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "idb": "^8.0.3"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "fake-indexeddb": "^6.0.1",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
